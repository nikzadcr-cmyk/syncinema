import 'package:flutter/material.dart';

class RoomActions extends StatelessWidget {
  const RoomActions({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
